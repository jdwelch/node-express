jdw/node-express:
  Build:
    - sudo apt-get -y install nodejs
    - sudo npm install -g yarn
    - yarn install
  PkgInclude:
    - '*'
  PreInstall:
    - sudo apt-get -y install nodejs
    - sudo npm install -g yarn
  Env:
    - PORT: "3000"
  Exec:
    - yarn start